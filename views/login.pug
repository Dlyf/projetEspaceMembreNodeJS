extends layouts/frontend

include includes/_mixins

block head
  //- Inject here JS and styles for this specific page

block content
  .ui.middle.aligned.center.aligned.grid
    .sixteen.wide.ten.wide.tablet.seven.wide.computer.column
      h1 Connexion

      if error
        .ui.red.message= error

      form.ui.large.form(method="post" action="/login")
        .ui.segment
          .field
            label Adresse E-mail
            input(type="email" name="email" placeholder="Votre adresse e-mail")

          .field
            label Mot de passe
            input(type="password" name="password" placeholder="Mot de passe")

          button(type="submit" class="ui fluid large teal submit button") Se connecter